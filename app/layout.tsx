import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "AsistControl",
  description: "AsistControl es una aplicación web diseñada para gestionar y monitorear los registros de ingreso y egreso del personal en tiempo real. Permite llevar un control preciso de la asistencia de los empleados, registrar horas trabajadas. Ideal para empresas que buscan optimizar la gestión horaria y mejorar la eficiencia operativa.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased min-h-screen`}
      >
        {children}
      </body>
    </html>
  );
}
